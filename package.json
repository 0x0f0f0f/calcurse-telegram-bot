{
  "name": "calcurse-telegram-bot",
  "version": "1.0.0",
  "description": "Telegram bot that parses and handles calcurse events and todos through a git (dotfiles) repository",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/calcurse-bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.0.0",
    "node-telegram-bot-api": "^0.30.0",
    "what-the-diff": "^0.6.0"
  },
  "devDependencies": {
    "eslint": "^6.2.1",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2"
  }
}
